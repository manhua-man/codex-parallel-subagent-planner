interface:
  display_name: Parallel Subagent Planner
  short_description: Decide when to use subagents, isolate file scopes, order dependencies, and write focused prompts.
  default_prompt: Decide whether subagents help with this task, and plan them only when useful.
