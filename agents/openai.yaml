interface:
  display_name: "Parallel Subagents"
  short_description: "Schedule safe Codex execution lanes and project waves."
  default_prompt: "Use $parallel-subagent-planner to schedule safe Codex execution lanes from an accepted goal, map project dependencies when needed, and integrate each wave."
