interface:
  display_name: "Parallel Subagents"
  short_description: "Cost-aware Codex split gate"
  default_prompt: "Use $parallel-subagent-planner as a cost-aware split check: launch only bounded Codex lanes, integrate and verify in the main thread, then ask before promoting any reusable long-term agent role."
